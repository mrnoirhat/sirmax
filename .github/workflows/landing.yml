# SPDX-License-Identifier: AGPL-3.0-or-later
name: Landing

on:
  push:
    branches: [experiment, testing, main]
    paths:
      - "apps/landing/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/landing.yml"
  # No paths filter on pull_request, deliberately. These four workflows are the required
  # checks on main, and GitHub waits forever for a required check that never reports — so a
  # path-filtered PR trigger turns "protected" into "unmergeable" for any change that misses
  # this directory. Pushes keep their filter, where skipping is free of that consequence.
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: landing-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Lint, typecheck & build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint --workspace apps/landing

      - name: Typecheck
        run: npm run typecheck --workspace apps/landing

      - name: Build (static export)
        run: npm run build --workspace apps/landing

      - name: Upload static site
        uses: actions/upload-artifact@v7
        with:
          name: landing-out
          path: apps/landing/out
          if-no-files-found: error
